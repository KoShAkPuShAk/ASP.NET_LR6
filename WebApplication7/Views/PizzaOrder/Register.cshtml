@model WebApplication7.Models.User

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pizza Order Registration</title>
    <style>
        body {
            background-color: #2c3e50; /* Midnight Blue */
            text-align: center;
            padding: 50px;
            font-family: Arial, sans-serif;
            color: #ecf0f1; /* Clouds */
        }

        h1 {
            font-size: 2em;
            margin-bottom: 20px;
            color: #e74c3c; /* Alizarin Crimson */
        }

        form {
            display: inline-block;
            text-align: left;
        }

        label {
            font-size: 1.5em;
            display: block;
            margin-bottom: 10px;
            color: #ecf0f1; /* Clouds */
        }

        input[type="text"],
        input[type="number"] {
            font-size: 1.5em;
            padding: 10px;
            margin-bottom: 20px;
            width: 300px;
        }

        button[type="submit"] {
            font-size: 1.5em;
            padding: 10px 20px;
            background-color: #3498db; /* Belize Hole */
            border: none;
            border-radius: 5px;
            cursor: pointer;
            color: #ecf0f1; /* Clouds */
            transition: background-color 0.3s ease-in-out, color 0.3s ease-in-out;
        }

        button[type="submit"]:hover {
            background-color: #2980b9; /* Belize Hole on Hover */
            color: #fff; /* White on Hover */
        }
    </style>
</head>
<body>
    <h1>Order Registration</h1>
    <form method="post" action="/PizzaOrder/ConfirmOrder">
        <label for="name">Name:</label>
        <input type="text" id="name" name="Name"><br>
        <label for="age">Age:</label>
        <input type="number" id="age" name="Age"><br>
        <button type="submit">Submit</button>
    </form>
</body>
</html>

